name: Build and Submit iOS

on:
  push:
    branches: ['release']

jobs:
  build:
    name: Build iOS
    type: build
    params:
      platform: ios
      profile: production
  
  submit:
    name: Submit iOS
    needs: build
    type: submit
    params:
      platform: ios
      profile: production